{
  "name": "sleep-tracker",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "fix": "gts fix",
    "pre-compile": "yarn clean",
    "post-compile": "cp -r src/views build/src && cp -r secret build",
    "compile": "yarn pre-compile && tsc && yarn post-compile",
    "start": "node build/src/index.js",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^13.11.1",
    "gts": "^2.0.2",
    "ts-node": "^8.10.2",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "googleapis": "^52.1.0",
    "got": "^11.3.0",
    "moment": "^2.27.0",
    "moment-timezone": "^0.5.31",
    "pug": "^3.0.0"
  }
}
